# QuNex unit tests readme

Tests should be executed from the `python` subfolder. So:

``` bash
cd $QUNEXREPO/python
pytest
```

For specific tests run:

``` bash
cd $QUNEXREPO/python
pytest tests/test_bids_sorting.py
```
